<%args>
$s
$user => undef
$password => undef
$host => undef
$port => undef
</%args>
<%init>
if ($user)
{
    $s->set_user($user);
    $s->set_password($password);
    $s->connect( host => $host, port => $port );
}
</%init>