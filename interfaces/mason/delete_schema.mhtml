<%args>
$schema
</%args>

<%init>
use Alzabo::Config;

my $s;
eval
{
    $s = Alzabo::Create::Schema->load_from_file( name => $schema );

    $s->delete;
};

$m->comp( '../common/exception', $@ ) if $@;

$m->comp( '../common/redirect', url => 'index' . Alzabo::Config::mason_extension() );

</%init>

<%flags>
inherit => 'syshandler'
</%flags>
