Success!

<%args>
$s
$user
$password
</%args>

<%init>

eval
{
    $s->create( user => $user,
		password => $password );
    $s->save_to_file;
};

$m->comp( &Alzabo::Config::mason_url_path . '/common/exception', $@ ) if $@;

</%init>
